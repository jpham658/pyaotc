#include <assert.h>


// int *range(int arr[], int start, int stop, int step)
// {
//     int idx = 0;
//     for (int i = start; i <= stop; i = i + step)
//     {
//         // TODO: Add check in for invalid arr size given
//         arr[idx] = i;
//         idx += 1;
//     }

//     return arr;
// }

int main()
{
    // int start = 1;
    // int stop = 4;
    // int step = 2;
    // int range_size = (start + stop) / step;
    int range_size = 2;
    int rangearr[range_size];

    // // range(1, 4, 2) == [1, 3]
    // int* filled_range = range(rangearr, start, stop, step);
    // for(int i = 0; i < sizeof(filled_range) / sizeof(int); i++) {
    //     printf("%d\n", filled_range[i]);
    // }
    // printf("size of range: %d\n", (start + stop) / step);
    return 0;
}